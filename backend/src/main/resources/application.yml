spring:
  application:
    name: backend
  docker:
    compose:
      enabled: false
  profiles:
    active: "@spring.profiles.active@"
  datasource:
    driver-class-name: org.postgresql.Driver


  
